# ============================================================================
# Harbonline Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control
#
# For Vercel deployment:
# - Add these variables in the Vercel dashboard under Settings → Environment Variables
# - Set appropriate scopes: Development / Preview / Production
# ============================================================================

# ----------------------------------------------------------------------------
# App Configuration
# ----------------------------------------------------------------------------
# Environment: local | development | staging | production
APP_ENV=local

# Node environment (set automatically by Next.js)
# NODE_ENV=development

# ----------------------------------------------------------------------------
# Firebase Client SDK (Public - sent to browser)
# ----------------------------------------------------------------------------
# Get these from Firebase Console → Project Settings → Your apps → Web app
# https://console.firebase.google.com

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# ----------------------------------------------------------------------------
# Firebase Admin SDK (Server-only - never sent to browser)
# ----------------------------------------------------------------------------
# Get these from Firebase Console → Project Settings → Service accounts
# Generate new private key → Download JSON
#
# IMPORTANT: For FIREBASE_ADMIN_PRIVATE_KEY:
# - Copy the entire private key including -----BEGIN PRIVATE KEY----- and -----END PRIVATE KEY-----
# - Keep the \n characters as-is (they will be converted automatically)
# - On Vercel, paste the entire key as a single line

FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# ----------------------------------------------------------------------------
# Google Analytics (GA4)
# ----------------------------------------------------------------------------
# Get this from Google Analytics → Admin → Data Streams → Web
# Format: G-XXXXXXXXXX
# Only loads in production when APP_ENV=production

NEXT_PUBLIC_GA_MEASUREMENT_ID=

# ----------------------------------------------------------------------------
# Google Search Console
# ----------------------------------------------------------------------------
# Get this from Google Search Console → Settings → Verification
# HTML tag method → Copy the content value from the meta tag
# Format: google1234567890abcdef.html (without the meta tag)

NEXT_PUBLIC_SEARCH_CONSOLE_VERIFICATION=

# ============================================================================
# Notes:
# ============================================================================
#
# Local Development:
# 1. Copy this file to .env.local
# 2. Fill in your development Firebase credentials
# 3. Leave analytics blank (disabled in local environment)
# 4. Restart your dev server after changes
#
# Vercel Deployment:
# 1. Go to your Vercel project → Settings → Environment Variables
# 2. Add each variable separately
# 3. Set correct scope for each:
#    - Development: Used when running `vercel dev`
#    - Preview: Used for preview deployments (PR branches)
#    - Production: Used for production deployment
# 4. For Firebase, you can use the same project or separate projects per environment
#
# Security:
# - Never commit .env.local to git
# - .env.local is already in .gitignore
# - Client variables (NEXT_PUBLIC_*) are bundled into the browser
# - Server variables (no prefix) stay server-side only
#
# ============================================================================
